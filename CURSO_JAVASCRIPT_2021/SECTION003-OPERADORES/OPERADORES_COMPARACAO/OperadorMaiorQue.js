var var1 = 3;
var var2 = 4;

/**
 * * OPERADOR: Maior que (>)
 * 
 * DESCRIÇÃO: Retorna verdadeiro caso o operando da esquerda seja maior que o da direita.
*/

if(var2 > var1) {
    console.log("Operador: maior que (>). var2 > var1. operando var2 e maior que operando var1.");
}
if("12" > 2) {
    console.log("Operador: maior que (>). \"12\" > 2. operando \"12\" > 2. operando \"12\" e maior que 2.");
}