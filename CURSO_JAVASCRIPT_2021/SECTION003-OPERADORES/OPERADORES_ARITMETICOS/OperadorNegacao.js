/**
 * * OPERADOR: NEGAÇÃO(-)
 * 
 * * DESCRIÇÃO: Operador unário. Retorna a negação de seu operando.
 * 
 * EXEMPLO: Se x é 3, então -x retorna -3.  
*/

//negação de numero
let x2 = 3;

console.log("Operador: negação(-), negação de numero. se x2 nornal resulta em = ",x2," usando o operador de negação: -x2 resulta em: ", -x2);

//negação de booleanos //0 = false 1 = true
let verdade = true;


console.log("Operador: negação(-), negação de booleanos. se a variavel verdade normal resulta em = ",verdade," usando o operador de negação em: verdade resulta em: ",-verdade);

//negação de strings
let str = 'string';

console.log("Operador: negação(-), negação de string. se a variavel str normal resulta em = ",str," usando o operador de negação em: str resulta em: ",-str);